# This secret should be created using kubectl command:
# kubectl create secret docker-registry registry-tikrai-secret \
#   --docker-server=ghcr.io \
#   --docker-username=<github-username> \
#   --docker-password=<github-personal-access-token> \
#   --docker-email=<your-email> \
#   --namespace=default
#
# Note: For GitHub Container Registry, use GitHub Personal Access Token (PAT) with 'read:packages' permission
# Application is now in default namespace, so --namespace=default is optional
#
# This file is kept for reference only - do not apply it directly

